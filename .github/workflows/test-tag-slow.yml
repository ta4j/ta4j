name: Run Slow Tagged Tests

permissions:
  contents: read

on:
  schedule:
    - cron: "37 4 * * *"
    - cron: "37 5 * * 1"
    - cron: "37 6 1 * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test-slow:
    name: Build and Test (slow tag)
    runs-on: ubuntu-latest
    if: >-
      ${{
        github.event_name == 'workflow_dispatch' ||
        (
          vars.TA4J_TAGGED_TEST_SLOW_SCHEDULE_ENABLED == 'true' &&
          (
            (vars.TA4J_TAGGED_TEST_SLOW_SCHEDULE_SLOT == 'daily' && github.event.schedule == '37 4 * * *') ||
            (vars.TA4J_TAGGED_TEST_SLOW_SCHEDULE_SLOT == 'weekly' && github.event.schedule == '37 5 * * 1') ||
            (vars.TA4J_TAGGED_TEST_SLOW_SCHEDULE_SLOT == 'monthly' && github.event.schedule == '37 6 1 * *')
          )
        )
      }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build and run slow tagged tests with Maven
        run: xvfb-run mvn -B test -Dgroups=slow -Dta4j.excludedTestTags=
